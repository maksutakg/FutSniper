{
  "name": "fut-sniper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
